controller_manager:
  ros__parameters:
    update_rate: 100 # Hz

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    simple_effort_controller:
      type: effort_controllers/JointGroupEffortController


simple_effort_controller:
  ros__parameters:
    joints:
      - wheel_left_joint
      - wheel_right_joint